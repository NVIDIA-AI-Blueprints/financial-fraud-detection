{
    "paths": {
      "data_dir": "/data",
      "output_dir": "/data/trained_models"
    },
    
    "models": [
      {
        "kind": "XGBoost",
        "gpu": "single",
        "hyperparameters": {
          "max_depth": 6,
          "learning_rate": 0.2,
          "num_parallel_tree": 3,
          "num_boost_round": 512,
          "gamma": 0.0
        }
      }
    ]
  }
  